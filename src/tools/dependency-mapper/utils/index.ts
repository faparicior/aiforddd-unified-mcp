/**
 * Utility functions for the Dependencies Mapper MCP server
 */

/**
 * Example utility function - replace with your actual utilities
 */
export function exampleUtility(input: string): string {
  return `Processed: ${input}`;
}